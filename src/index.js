// Utils
import debugInstance from 'debug';
const debug = debugInstance('canvas:service:neurald');

// Power law
// Scale invariance
// 1000 brains
// Universe is a huge resonance chamber

export default class NeuralD {
    constructor(options = {}) {}
}
